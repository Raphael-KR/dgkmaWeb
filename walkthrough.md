# 개발·운영 기능 검증 가이드

이 문서는 Replit 개발 환경과 `https://dgkma.org` 운영 환경의 기능을 확인하는 수동 회귀 테스트 체크리스트입니다. 개발 중에는 `https://dc5e5541-525b-4ad6-b914-2d2db70cb4a9-00-flpzugprplfl.spock.replit.dev`를 먼저 확인하고, Republish 후에만 운영 URL을 확인합니다. 향후 기능과 우선순위는 [제품·개발 통합 계획서](./planning_proposal.md)를 참고합니다.

## QA 실행 원칙

- 개발 중에는 자동화 테스트, 타입 검사, 빌드, Development Database와 Replit 개발 서버 검증을 계속 수행한다.
- 개발 홈페이지에서 확인 가능한 항목은 Codex가 인앱브라우저로 직접 확인한다. 사용자에게 Republish를 요청하는 것은 운영 환경에서만 확인 가능한 항목으로 제한하고, 가능한 운영 확인을 한 번에 모은다.
- 이 문서의 실제 계정·운영 역할·데스크톱·모바일 수동 항목은 기능별로 사용자에게 반복 요청하지 않고 누적한다.
- 핵심 개발 범위가 끝나면 비로그인·일반회원·관리자 계정과 데스크톱·모바일 환경을 준비해 이 체크리스트를 통합 QA로 한 번에 실행한다.
- 사용자 본인만 처리할 수 있는 외부 서비스 설정, 개발 차단 요소, 보안 노출·운영 데이터 손상 위험, 제품 방향 결정은 통합 QA 전에도 즉시 확인한다.
- 체크되지 않은 항목은 실패를 뜻하지 않으며 통합 QA 대기 상태를 뜻한다. 실제 검증 전에는 완료로 기록하지 않는다.
- 자동화 회귀와 synthetic/client-navigation QA의 `[x]`는 실제 계정·운영 통합 QA의 완료를 뜻하지 않는다. 2026-07-27 현재 병합 후보의 실제 계정 allowlist/login/recovery, Production smoke, 실제 Kakao 통합 QA, Sheets 적용, payment provider·실제 결제는 계속 `[ ]`로 관리한다.

## 검증 전 조건

- [ ] GitHub `main`, Replit 개발 워크스페이스, 배포 대상 커밋을 확인한다.
- [ ] Replit 개발 워크스페이스에서 `npm test`가 종료 코드 `0`으로 끝난다.
- [ ] Replit 개발 워크스페이스에서 `npm run check`가 종료 코드 `0`으로 끝난다.
- [ ] Replit 개발 워크스페이스에서 `npm run build`가 종료 코드 `0`으로 끝난다.
- [ ] Replit 개발 워크스페이스에서 `git diff --check`가 종료 코드 `0`으로 끝난다.
- [ ] Replit Deployments에서 Republish가 완료되었다.
- [ ] 비로그인 브라우저와 실제 동문 계정을 준비한다.
- [ ] 관리자 기능 검증이 필요하면 현재 환경의 `KAKAO_*_ADMIN_USER_IDS`에 등록된 카카오 계정으로 다시 로그인해 `isAdmin=true` 복구를 확인한다.

## 공개 페이지

비로그인 브라우저에서 확인한다.

- [ ] `/`에서 공개 홈페이지가 표시된다.
- [ ] `/about/intro`에서 동문회 소개를 확인한다.
- [ ] `/about/executives`에서 임원진 정보를 확인한다.
- [ ] `/about/bylaws`에서 회칙을 확인한다.
- [ ] `/about/join`에서 가입 안내를 확인한다.
- [ ] `/about/dues`에서 회비 안내를 확인한다.
- [ ] `/about/condolence`에서 경조사 지원 안내를 확인한다.
- [ ] `/terms`와 `/privacy`가 로그인 없이 열린다.
- [ ] 회원 전용 메뉴를 선택하면 로그인 안내가 표시된다.
- [ ] 모바일에서 하단 내비게이션과 공개 페이지 내용이 겹치지 않는다.

## 카카오 로그인과 온보딩

현재 로그인은 카카오 REST OAuth 방식이다.

개발 OAuth는 공식 테스트 앱에서 설정 가능한 이름·전화번호·이메일·프로필 사진·생일만 요청한다. 운영 OAuth는 같은 항목에 운영 대표 채널용 `plusfriends`를 추가 요청한다. 개발 로그인에서 운영 앱 키를 공유하지 않는다.

### 수집 항목과 동의 범위

로그인 화면, 카카오 동의항목, 개인정보 처리방침의 설명이 아래 범위와 일치하는지 확인한다. 카카오 콘솔의 정확한 설정 문구와 제출 순서는 [카카오 동의항목 심사 가이드](./docs/kakao-consent-review-guide.md)를 따른다.

- [ ] 필수: 이름, 카카오계정(전화번호), 카카오계정(이메일). 이름·전화번호는 졸업생 명부 일치와 동문 자격 인증에, 이메일은 회원 식별·계정 관리·동문회 공식 안내에 사용한다고 표시된다.
- [ ] 선택: 프로필 사진, 생일, 카카오톡 채널 추가 상태 및 내역. 선택하지 않아도 가입할 수 있고, 각각 프로필 표시, 본인 전용 생일 축하 화면 또는 공식 채널 추가만 제공되지 않는다고 표시된다.
- [ ] 생일은 선택 동의이며, 생일 유형·윤달 여부가 전달될 때도 본인 전용 화면에만 사용된다.
- [ ] 채널 추가 상태 및 내역은 선택 동의이며 동의 목적은 `동문회 공지 및 경조사 알림`이다. 이 동의만으로 친구톡·알림톡 자동 발송 기능을 제공한다고 표시하지 않는다.
- [ ] CI(연계정보), 카카오 닉네임, 성별, 연령대, 출생 연도, 친구 목록, 카카오톡 메시지·캘린더 접근권한을 수집하거나 요청하지 않는다.
- [ ] 카카오가 callback URL의 query로 인가 코드와 state를 전달하고, 브라우저가 인가 코드와 state를 서버에 한 번 전달한다. 서버 응답과 앱 로그에 전체 인가 코드나 토큰을 남기지 않으며, 어드민 키와 카카오 원문 사용자 응답도 반환하거나 기록하지 않는다.
- [ ] `/privacy`의 `제5조의2`에 Replit·Neon의 국외 이전 국가, 연락처, 이전 항목, 목적, 시기·방법, 보유기간과 거부 시 제한이 표시된다.

### 명부 일치 계정

- [ ] 카카오 로그인 후 정상적으로 회원 세션이 생성된다.
- [ ] 이름과 휴대전화번호가 동문 명부와 일치하면 자동 가입된다.
- [ ] 첫 로그인 사용자의 `activityRegion`이 없으면 `/onboarding/region`으로 이동한다.
- [ ] 활동 지역 저장 후 원래 회원 화면으로 이동한다.
- [ ] 새로고침해도 로그인 세션이 유지된다.
- [ ] 로그아웃 후 회원 전용 페이지에 접근하면 다시 로그인 안내가 표시된다.

### 명부 불일치 계정

- [ ] 가입 승인 대기로 접수되고 `202` 응답과 승인 대기 안내가 표시된다.
- [ ] 사용자 계정과 회원 세션이 즉시 생성되지 않는다.
- [ ] 관리자 화면의 가입 승인 목록에 신청이 표시된다.

명부 불일치 사용자는 관리자가 승인 또는 거절한다.

- [ ] 가입 거절 시 카카오 연결 해제 성공 또는 이미 해제된 상태가 확인된 뒤 신청정보가 삭제되고 `{ deleted: true, id }`만 응답한다.
- [ ] 어드민 키 누락은 `500`, 카카오 연결 해제 실패는 `502`, 누락·형식 오류·열 불일치가 있는 legacy 카카오 회원번호는 `409`를 반환하며, 모두 거절 미완료로 처리하고 신청정보를 보존한다.

### 계약 회귀 자동화 (실제 계정 QA 아님)

2026-07-28 현재 병합 후보의 최종 동결 증거에서 다음 서버 계약을 자동 검증했다. 이 체크는 실제 Kakao 로그인·allowlist·가입 승인/거절 통합 QA를 대체하지 않는다.

- [x] 익명 same-cookie OAuth의 pending 등록 `202` 뒤 `/api/auth/me`가 정확히 `401`과 `{ "message": "Not authenticated" }`를 반환하고, pending 생성 `1`, member writes(사용자 생성·로그인 확정) `0`을 확인했다.
- [x] 사전 인증된 기존 회원 세션에서 시작 전 `/api/auth/me`가 `200`인 것을 확인한 뒤, 카카오 로그인 시작이 기존 `userId`를 제거·저장하고 다른 카카오 신원의 pending `202`로 진행하는지 확인했다. 이후 `/api/auth/me`는 `401`, pending 생성은 `1`, member writes와 로그인 확정 writes는 모두 `0`이었다.
- [x] 이 회귀는 `/api/auth/kakao/start` 로그인 시작 경계에서 이전 회원 세션을 해제·저장하는 보안 수정으로 막았다. 수정 전 실패하도록 고정한 test-only SHA 증거와 수정 후 통과 SHA 증거는 후보별 증거에 보존한다.
- [x] 성공한 카카오 회원 callback은 검증과 회원 확정 뒤 세션을 재생성하고 새 세션에만 `userId`를 저장한다. test-only SHA `227e99bfe8bf5dcfcc0f78f92f2bb906644cc6e2`의 선택 회귀는 새 인증 쿠키가 없어 `0/1`로 실패했고, fix SHA `c527e79ef7a71816bc35c2d08435cd9857e8b72e`에서는 이전 쿠키 `/api/auth/me` `401`, 새 쿠키 `200`으로 `1/1` 통과했다. 기존 성공·경쟁·승인 경로를 포함한 Kakao route 전체도 `26/26` 통과했다.
- [x] 관리자 결제 valid payload `{ "userId": 41, "amount": 50000, "year": 2026, "type": "연회비", "status": "completed", "receiptUrl": null }`가 `201`·write `1`이고, `amount` 누락은 `400`·errors 배열·write `0`이며 기존 비로그인 `401`·일반회원 `403` 무쓰기 회귀도 통과했다.
- [x] 이전 후보의 focused `60/60`·전체 `341/341` 근거는 성공 로그인 세션 회전 수정으로 최종 후보 근거에서 제외했다. 새 최종 후보의 정확한 focused·전체·JUnit·check·build·residue 수치와 SHA는 `최종 후보의 candidate-metadata.json으로 식별한 증거 루트`에서 확인한다.

## 게시판·댓글·이미지

실제 회원 계정으로 `/b`에 접근한다.

### 카테고리와 목록

- [ ] `전체`, `공지`, `자유`, `행사`, `소식` 탭이 표시된다.
- [ ] 탭을 바꾸면 해당 카테고리 게시글만 표시된다.
- [ ] `전체`는 목록 필터로만 표시되고 글쓰기 카테고리에서는 선택할 수 없다.
- [ ] `GET /api/categories`가 `all`, `notice`, `free`, `event`, `news`를 반환한다.
- [ ] 게시글 제목, 카테고리, 날짜가 목록에서 잘리지 않는다.

### 게시글

- [ ] 글쓰기 화면에서 `공지`, `자유`, `행사`, `소식` 중 하나를 선택할 수 있다.
- [ ] 제목과 내용을 입력해 게시글을 등록한다.
- [ ] 작성 완료 안내 후 새 글이 목록에 표시된다.
- [ ] 새 글 상세 화면에서 제목, 내용, 작성 정보가 표시된다.
- [ ] 검색 화면에서 제목 또는 본문 검색 결과를 확인한다.

### 댓글

- [ ] 게시글 상세에서 댓글을 작성한다.
- [ ] 작성한 댓글이 즉시 표시된다.
- [ ] 본인이 작성한 댓글을 삭제한다.
- [ ] 삭제한 댓글이 다시 표시되지 않는다.
- [ ] 다른 일반회원의 댓글은 삭제할 수 없다.

### 이미지 첨부

- [ ] 170KB 수준의 JPEG 이미지를 첨부해 게시글을 작성한다.
- [ ] 5MB 수준의 PNG 이미지를 첨부해 게시글을 작성한다.
- [ ] 게시글 상세에서 첨부 이미지가 표시된다.
- [ ] JPG, PNG, WebP, GIF 형식만 업로드 URL을 발급받을 수 있다.
- [ ] 10MB 초과 파일은 업로드 전에 오류 메시지를 표시한다.
- [ ] SVG 또는 허용되지 않은 MIME 형식은 오류 메시지를 표시한다.
- [ ] 업로드 실패 시 실패 이유가 토스트 설명에 표시된다.

## 동문 주소록

실제 회원 계정으로 `/directory`에 접근한다.

- [ ] 로그인하지 않은 요청은 `401`을 반환한다.
- [ ] 본인의 기수 또는 활동 지역에 해당하는 동문만 목록에 표시된다.
- [ ] 이름과 기수로 현재 범위 안에서 검색할 수 있다.
- [ ] 이름, 기수, 학과, 졸업년도, 동문회 직책만 목록에 표시된다.
- [ ] 주소, 휴대전화번호, 전화번호, 메모가 API 목록 응답에 포함되지 않는다.
- [ ] 기수와 활동 지역을 산출할 수 없는 회원에게 빈 목록과 안내가 표시된다.
- [ ] 관리자는 전체 범위를 조회할 수 있다.

## 부고

실제 회원 계정으로 `/o`와 `/o/new`에 접근한다.

- [ ] 비로그인 `GET /api/obituaries` 요청이 `401`을 반환한다.
- [ ] 비로그인 `GET /api/obituaries/:id` 요청이 `401`을 반환한다.
- [ ] 비로그인 `POST /api/obituary/parse` 요청이 `401`을 반환한다.
- [ ] 비로그인 `POST /api/obituaries` 요청이 `401`을 반환한다.
- [ ] 로그인 회원은 부고 목록·상세·문자 파싱·등록을 사용할 수 있다.

### 정규식 기반 문자 파싱

- [ ] 부고 문자에 `故 홍길동` 또는 `고인: 홍길동` 형식을 넣고 분석한다.
- [ ] 관계, 별세일, 빈소, 장지, 상주, 계좌, 연락처 중 감지 가능한 항목이 폼에 채워진다.
- [ ] 자동으로 채워지지 않은 항목을 직접 수정할 수 있다.
- [ ] 링크 내용 수집이나 생성형 AI 추론이 제공되지 않는다는 점을 확인한다.

### 등록과 상세

- [ ] 필수 항목을 입력해 부고를 등록한다.
- [ ] `/o` 목록에 새 부고가 표시된다.
- [ ] `/o/:id` 상세에서 고인, 별세일, 빈소, 장지, 상주가 표시된다.
- [ ] 연락처가 있으면 전화 링크가 동작한다.
- [ ] 계좌번호가 있으면 복사 기능이 동작한다.

부고 데이터 API는 회원 전용이며 공개 경조사 안내 페이지 `/about/condolence`와 별개다.

## 경조사 통합 기반 API (진행 중)

다음 항목은 회원 전용 기반 API의 계약과 접근 정책을 확인하는 체크다. `/events` UI와 문자·링크 파싱은 아래 별도 절에서 확인한다.

- [ ] 비로그인 `GET /api/events`와 `GET /api/events/:id` 요청이 `401`을 반환한다.
- [ ] 비로그인 초안 조회·생성·수정·삭제와 게시 전환 요청이 `401`을 반환한다.
- [ ] 실제 회원이 `POST /api/events/drafts`로 자신의 초안을 만든다.
- [ ] 작성자 회원은 자신의 초안을 수정·삭제하고 게시할 수 있다.
- [ ] 다른 회원은 해당 초안을 수정·삭제·게시할 수 없고 `404`를 받는다.
- [ ] `GET /api/events`와 `GET /api/events/:id`는 `published` 상태만 반환한다.
- [ ] `GET /api/events?type=obituary`, `wedding`, `opening`, `other`는 요청 유형에 맞는 published 경조사만 반환한다.
- [ ] published 목록·상세 응답에 원문 `sourceText`가 포함되지 않는다.

기존 부고 API와 데이터는 별도로 유지한다. `community_events` 스키마와 부고 복사 SQL은 개발·프로덕션 SQL 콘솔에서 별도 승인 후 적용하며, 코드 시작이나 테스트가 마이그레이션을 자동 실행하지 않는지 확인한다.

## 경조사 등록·초안 UI (진행 중)

이 절은 `/events` 화면과 초안 흐름의 실제 회원 브라우저 검증용이다. 현재 구현은 코드와 Replit 검증 단계이며, Republish와 실제 회원 검증 전에는 프로덕션 완료로 기록하지 않는다.

### 데스크톱

실제 회원 계정으로 `/events`에 접근한다.

- [ ] 목록 위에 `부고`, `결혼`, `개원`, `기타` 유형 선택, 문자·링크 입력란, 선택 유형에 맞는 등록 명령만 간결하게 표시된다.
- [ ] 등록 영역에는 별도 `경조사 등록` 제목이나 사용 안내문이 표시되지 않는다.
- [ ] 등록 명령을 누르기 전에는 상세 필드와 미리보기가 표시되지 않는다.
- [ ] 선택 유형의 등록 명령을 누르면 같은 화면에 상세 확인 영역이 열리고 유형별 필드가 표시된다.
- [ ] 문자와 공개 URL을 함께 입력해 등록 명령을 실행하면 문자와 링크 본문을 함께 분석하고, 입력 원문은 바뀌지 않는다.
- [x] URL만 입력해도 공개 HTML 또는 일반 텍스트 본문을 분석하고 출처 상태가 표시된다.
- [ ] 만료되거나 접근할 수 없는 URL은 안전한 실패 상태를 표시하고, 함께 입력한 문자는 계속 분석한다.
- [ ] `localhost`, loopback, 사설망·링크 로컬 주소는 수집하지 않고 차단 상태를 표시한다.
- [x] 부고에서 나이·관계·발인·빈소 등 필수 정보가 부족하면 확인 영역에 누락 항목이 표시된다.
- [ ] 목록 필터 없이 모든 유형의 경조사가 함께 표시되고, 각 카드에서 유형을 구분할 수 있다.
- [ ] 유형별 필수 항목을 비운 채 게시하면 각 필드 가까이에 누락 오류가 표시된다.
- [ ] 의미 있는 입력 뒤 `저장 중`과 `임시저장됨` 상태가 순서대로 표시된다.
- [ ] 새로고침 후 같은 유형의 초안이 복구되고 `임시저장된 내용을 복구했습니다` 상태가 표시된다.
- [ ] 실제 회원이 `부고`, `결혼`, `개원`, `기타` 각각에 구별 가능한 의미 있는 입력을 저장하고, 유형을 번갈아 전환하고 새로고침한다.
- [ ] 각 유형에서 마지막으로 저장한 초안이 정확히 하나씩 독립적으로 복구되고, 다른 유형의 입력·초안 ID·내용이 덮어써지지 않는다.
- [ ] 복구된 초안을 삭제하면 입력값과 초안 상태가 초기화되고, 다시 새로고침해도 복구되지 않는다.
- [ ] 가능한 범위에서 네트워크 또는 응답 실패를 관찰하거나 유도해 저장 또는 복구 실패를 확인한다. 오류가 보이고 `다시 시도` 명령이 표시된다.
- [ ] `다시 시도`가 성공하면 입력값과 현재 초안 ID가 유지되고, 같은 유형에 중복 초안이 새로 만들어지지 않는다.
- [ ] 부고 초안의 필수 정보가 갖춰지면 표준 부고문 미리보기가 표시되고, 미리보기의 복사 버튼이 내용을 클립보드에 복사한다.
- [ ] 게시하면 `/events`에 남아 있고 성공 안내 뒤 새 published 카드가 현재 목록에 즉시 나타난다.
- [ ] 게시 뒤 등록 도구는 같은 유형의 빈 새 입력 상태가 되고 이전 초안은 다시 복구되지 않는다.
- [ ] `/b`의 `경조사` 진입점과 회원 홈의 `경조사` 진입점이 모두 `/events`로 이동한다.
- [ ] `/o`는 `/events?type=obituary`, `/o/new`는 `/events?type=obituary&compose=1`로 이동한다.
- [ ] 기존 `/o/:id` 부고 상세는 legacy 상세 화면으로 계속 열리고, 공개 `/about/condolence`는 로그인 없이 계속 열린다.

### 모바일

실제 회원 계정으로 좁은 모바일 폭에서 위 흐름을 반복한다.

- [ ] 간결한 등록 입력과 등록 후 열리는 상세 확인 영역이 가로 스크롤이나 겹침 없이 읽힌다.
- [ ] 필터 없는 전체 목록의 모든 카드가 터치로 선택되며, 게시 직후 새 카드 표시가 안정적으로 동작한다.
- [ ] 복구된 부고 미리보기와 복사 버튼이 잘리지 않고 조작 가능하며, 초안 삭제·새로고침 복구·재시도 흐름이 데스크톱과 동일하게 동작한다.
- [ ] 가능한 범위에서 네트워크 또는 응답 실패를 관찰하거나 유도해 저장 또는 복구 실패를 확인한다. 모바일에서도 오류와 `다시 시도` 명령이 보인다.
- [ ] 모바일에서 `다시 시도`가 성공해도 입력값과 현재 초안 ID가 유지되고, 같은 유형의 중복 초안이 새로 만들어지지 않는다.
- [ ] `/b`, 회원 홈, `/o`, `/o/new`, `/o/:id`, `/about/condolence` 진입과 리디렉션이 모바일에서도 데스크톱과 같은 결과를 낸다.

### 문자·공개 링크 파싱 자동화 점검

다음 항목은 코드·개발 환경 자동화로 먼저 확인하고 실제 회원 화면은 위 데스크톱·모바일 통합 QA에서 다시 확인한다.

- [x] 비로그인 `POST /api/events/parse`는 `401`을 반환한다.
- [x] 문자만, 공개 URL만, 문자와 URL 혼합 입력을 모두 처리하고 입력 원문을 초안에 그대로 보존한다.
- [x] 최대 3개 URL만 순서대로 처리하고 요청당 입력 길이와 호출 횟수를 제한한다.
- [x] DNS의 모든 응답 주소를 검사하고 검증한 주소에 연결을 고정하며, 리디렉션마다 같은 검증을 반복한다.
- [x] loopback·사설망·link-local·문서용·예약 주소와 비표준 포트·사용자정보 포함 URL을 차단한다.
- [x] HTML·일반 텍스트만 허용하고 압축 응답, 과대 응답, 지연된 헤더·본문과 과도한 리디렉션은 안전하게 중단한다.
- [x] 외부 오류 본문·내부 주소·스택을 클라이언트에 노출하지 않고 고정된 출처 상태만 반환한다.
- [x] 공개 링크 실패 시 함께 입력한 문자 분석을 유지하고, 부고 필수 정보 누락 목록을 반환한다.
- [x] 과도하게 중첩되거나 요소가 많은 HTML은 DOM 생성 전에 중단한다.
- [x] DNS·리디렉션·본문 읽기를 포함한 서버 전체 deadline과 클라이언트 연결 종료 신호가 진행 중 요청과 남은 URL 처리를 중단한다.
- [x] 초안 생성·수정·게시 API가 파싱 API를 우회한 사설 주소를 거부하고, 저장된 기존 비공개 주소도 회원 응답에서 제거한다.
- [x] PostgreSQL의 회원별 공유 호출 제한에서 허용량을 넘은 요청은 `429`를 반환하고, 각 인스턴스는 회원별·전체 동시 작업 수도 제한한다.

이 체크는 외부 사이트별 파싱 정확도나 실제 회원 브라우저 QA 완료를 뜻하지 않는다. 대표 공개 링크·만료 링크와 실제 부고문은 개발 서버에서 확인한 뒤 통합 QA 항목에 누적한다.

2026-07-14 Replit Development Database의 익명 임시 회원·명부·세션으로 네 유형을 실제 개발 서버 API에서 초안 생성·게시했다. 게시 목록 4건, `sourceText` 비노출, `example.com` 공개 본문 수집, loopback 차단과 구조화된 사설 URL 초안 `400`을 확인했다. 검증 후 임시 사용자·명부·세션·경조사와 고아 제한 행은 모두 `0`건으로 정리했다. 이는 실제 카카오 회원 화면의 데스크톱·모바일 통합 QA를 대체하지 않는다.

2026-07-16 Production Database의 `event_parse_rate_limits` 스키마를 확인한 뒤 Republish했다. 운영 실제 회원으로 `/events`의 유형 선택·문자 입력·분석 완료·필수 누락 안내·초안 자동저장·삭제를 확인했고, 배포 JavaScript 자산의 SHA-256이 Replit 빌드와 일치하며 브라우저 경고·오류가 없음을 확인했다. 검증 후 운영 `community_events`와 초안은 모두 `0`건이며, 호출 제한 상태 1건만 정상적으로 남았다. 공개 링크별 정확도와 전체 게시 흐름은 통합 QA에서 확인한다.

2026-07-29 Development 실제 회원으로 `chumo.daqda.kr` 공개 부고 링크를 입력했을 때 앱 수집기는 `User-Agent` 없이 요청해 원본의 `500` 응답을 받았고, 링크를 `unavailable`로 오판했다. 같은 Replit 환경에서 `User-Agent`가 없으면 `500`, 개인정보 없는 고정 식별자를 보내면 `200`이 되는 것을 반복 확인했다. `requestPublicAddress`가 식별자를 보내야 통과하는 회귀 테스트를 먼저 추가했으며, 수정 전 Replit 집중 테스트는 해당 사례만 `500 !== 200`으로 실패하고 기존 15개는 통과했다.

고정 파서 식별자를 추가한 뒤 실제 링크 수집은 `200`으로 바뀌었지만 본문 추출 결과가 빈 문자열인 두 번째 결함을 확인했다. 원본의 표시 콘텐츠 전체가 ASP.NET `<form>` 안에 있는데 기존 정제기가 `form` 요소와 하위 본문을 통째로 제거했다. 폼 안의 공개 부고 본문은 보존하고 입력·버튼·레이블만 제외해야 하는 회귀 테스트를 추가했으며, 수정 전 Replit 집중 테스트는 해당 사례만 빈 문자열로 실패하고 기존 5개는 통과했다.

폼 본문 정제 수정 뒤 실제 링크는 `fetched`로 전환되고 발인·빈소·장지·연락처를 추출했으나, `향년` 없는 `76세/ 남` 형식의 나이는 누락하고 `상주 정보` 제목의 `정보`를 상주로 잘못 추출했다. 두 실제 형식을 각각 회귀 테스트로 추가했으며, 수정 전 Replit 부고 파서 집중 테스트는 신규 2건만 `undefined !== 76`, `정보 !== 상주명`으로 실패하고 기존 7개는 통과했다.

나이·상주 수정 뒤 실제 브라우저에서 원본의 계좌 선택 안내문을 계좌번호로 저장하고, 줄이 분리된 장례식장 호실을 누락하는 추가 결함을 확인했다. 숫자 계좌 근거가 없는 안내문은 저장하지 않고, 빈소명 바로 다음 `호실` 줄은 결합해야 하는 회귀 테스트를 추가했으며, 수정 전 Replit 부고 파서 집중 테스트는 신규 2건만 실패하고 기존 9개는 통과했다.

최종 집중 테스트는 Replit에서 33/33 통과했다. 수정된 개발 서버의 실제 회원 브라우저에서 같은 링크가 `fetched`로 표시되고, 고인·나이·발인·빈소 호실·장지·상주·연락처와 원문 URL을 추출했으며 계좌 안내문은 저장하지 않았다. 새로고침 뒤 원문 복구와 재분석 후에도 초안 ID 309 하나만 유지됐다. 원문에 동문과 고인의 관계가 없어 해당 항목만 정확히 누락 안내되고 게시가 비활성화됐으며, 추측한 관계로 게시하지 않았다.

전체 회귀 검증은 Replit에서 `npm test` 347/347, `npm run check`, `npm run build`, `git diff --check`가 모두 종료 코드 `0`으로 통과했다. 로그인 브라우저의 경고·오류는 0건이었고 실행 로그에는 링크 식별자, 전화번호·계좌번호 형태, 장례식장 주소, 원문 필드 또는 오류 행이 기록되지 않았다. 테스트 중 만든 초안 ID 307~309만 삭제한 뒤 Development Database 조회 결과가 0건임을 확인했다. 필수 관계가 원본에 없어 게시·목록·상세는 이 링크로 실행하지 않았으며, Production·Production Database·Republish는 사용하지 않았다.

2026-07-30 같은 공개 링크에 `동국한의 07학번 OOO동문의 부친께서 YYYY년 M월 D일 별세` 문자를 함께 입력했다. 링크는 `fetched`였지만, 파서가 관계와 관련 동문을 모두 누락해 제목을 `부고`로 두고 게시를 차단했다. 날짜가 관계와 별세 표현 사이에 있는 문장과 `동문의 부친께서` 소유격 문형을 각각 회귀 테스트로 먼저 추가했으며, 수정 전 Replit 집중 테스트는 신규 2건만 actual `undefined`로 실패하고 기존 11건은 통과했다. 문자에 적힌 별세일과 링크의 발인일은 서로 다른 의미이며, 경조사 `eventDate`와 필수 상세 날짜가 발인일인 기존 계약은 유지한다.

최소 파서 수정 뒤 Replit 집중 테스트 13/13이 통과했고, 같은 실제 혼합 입력에서 제목·관련 동문·관계가 문자 근거와 일치하며 링크의 발인·빈소·고인·나이·장지·상주·연락처를 유지했다. 누락 경고가 사라지고 미리보기와 게시 버튼도 활성화됐다. 그러나 관련 동문이 로그인 작성자와 다른 경우 표준 미리보기와 게시 API가 보안 계약에 따라 작성자 회원정보로 정규화되어, 입력한 관련 동문이 아닌 작성자의 이름·기수·연락처가 표시됐다. 잘못된 부고문 게시를 피하기 위해 게시·목록·상세 검증을 중단했으며, 대리 등록 허용 또는 본인 경조사만 허용하는 불일치 차단 중 제품 정책을 먼저 확정해야 한다.

이후 일반회원은 본인 경조사만 등록하고, 관리자는 입력한 동문 이름과 학번이 명부에서 정확히 한 명과 일치할 때만 대리 등록하도록 정책을 확정했다. 일반회원의 타인 등록 차단, 관리자의 단일 일치 허용, 중복 일치 차단, 학번 누락 차단, 아직 로그인하지 않은 명부 동문의 대리 등록을 회귀 테스트로 먼저 작성했으며 새 정책 모듈이 없는 RED 실패를 확인했다. 서버는 초안 소유자와 경조사 대상자를 분리해 판정하고, 대리 등록 시 대상 동문의 명부·연결 회원·회원등급을 표준 부고문에 사용한다. 연결 회원이 없는 정확한 명부 일치는 일반회원으로 처리하며 명부 연락처를 사용한다. 로컬 집중 테스트 10/10은 통과했으나 Replit 개발 서버가 중지되고 SSH 공개키 인증도 거부된 상태여서 전체 회귀와 실제 입력 재검증은 아직 완료하지 않았다.

## 프로필·권리회원

실제 회원 계정으로 `/profile`에 접근한다.

- [ ] 이름, 프로필 이미지, 졸업년도와 현재 회원 상태가 표시된다.
- [ ] 활동 지역과 허용된 프로필 항목을 수정한다.
- [ ] 이름, 졸업년도, 연락처처럼 검증된 항목은 일반 프로필 수정 요청으로 바꿀 수 없다.
- [ ] 당해년도 완료된 `연회비` 기록 합계가 기준액 이상이면 `권리회원`으로 표시된다.
- [ ] 기준을 충족하지 않으면 `일반회원`으로 표시된다.

현재 권리회원 표시는 내부 납부 기록을 사용하며 실제 결제 승인이나 정기결제 기능을 의미하지 않는다.

## 회원 탈퇴

실제 개발 계정과 Development Database에서 수행한다. 탈퇴는 되돌릴 수 없으므로, 실행 전 대상 계정과 관련 데이터의 수를 기록하고 테스트 계정만 사용한다. 개발 환경의 `KAKAO_DEV_ADMIN_KEY`가 있어야 카카오 연결 해제를 포함한 실제 흐름을 확인할 수 있다.

- [ ] 탈퇴 전 새 DB 연결에서 대상 사용자의 `users`, `session`, `alumni_database.matched_user_id`, `pending_registrations`, `community_events`, 게시글·댓글·부고, 결제 기록 연결 수를 기록한다.
- [ ] 로그인한 상태에서 `내 정보 → 설정 → 회원 탈퇴`로 이동한다.
- [ ] 삭제·보존 안내와 확인 입력이 표시되고, 정확히 `탈퇴`를 입력하기 전에는 최종 버튼이 비활성화된다.
- [ ] `DELETE /api/users/me`가 세션의 사용자만 대상으로 하며, 비로그인 요청은 `401`, 다른 확인 문구는 `400`을 반환한다.
- [ ] 정상 탈퇴에서 카카오 연결 해제 후 로컬 회원 개인정보가 삭제되고 세션과 `connect.sid` 쿠키가 정리된다. 카카오 연결 해제가 실패하면 로컬 삭제가 진행되지 않는다.
- [ ] 탈퇴 직후 새 브라우저 세션에서 `GET /api/auth/me`가 `401`을 반환한다.
- [ ] 새 DB 연결에서 대상 사용자와 대상 세션이 `0`건이고, 동문 명부 연결은 해제됐으며, 미발행 경조사 초안은 삭제됐는지 확인한다.
- [ ] 게시글·댓글·발행된 경조사·기존 부고는 작성자 연결만 제거된 익명 상태로 유지되고, 결제 기록은 사용자 연결만 제거됐는지 확인한다.

카카오 연결 해제 또는 로컬 삭제가 실패한 경우, 성공으로 기록하지 않는다. 안전한 오류 안내가 표시되고 대상 계정이 남아 있는지 확인한 뒤 원인을 해결하고 처음부터 재검증한다.

## 관리자 기능 주의사항

관리자 화면과 `/api/admin/*`는 서버에서도 관리자 계정을 요구한다. 실제 결제 연동 전까지 결제 기록 생성도 관리자만 수행한다.

- [ ] Development와 Production에서 각각 현재 환경 allowlist의 카카오 계정으로 로그인하면 `isAdmin=true`가 복구되고, DB 초기화·계정 재생성 뒤에도 같은 동작을 한다.
- [ ] allowlist Secret이 없으면 자동 승격하지 않고, 다른 절차로 부여한 기존 관리자 권한은 allowlist에 없다는 이유로 자동 회수하지 않는다.
- [ ] 관리자 계정의 커뮤니티 홈 상단 `관리자` 배지가 클릭 가능한 상태로 표시되고 `/admin` 관리자 패널로 이동한다. 일반회원에게는 이 링크가 표시되지 않는다.
- [ ] 비로그인·일반회원의 모든 `/api/admin/*` 요청이 각각 `401`, `403`을 반환하며, 특히 `POST /api/admin/sync-alumni/preview`와 `POST /api/admin/sync-alumni`가 DB를 변경하지 않는다.
- [ ] 관리자 화면에서 가입 승인과 Google Sheets 연결 확인이 정상 동작한다.
- [ ] `변경 미리보기`는 원본·DB·추가·수정·동일·충돌·오류·원본만·DB만 건수와 fingerprint만 반환하고 이름·전화번호·주소·메모를 브라우저나 응답에 노출하지 않는다.
- [ ] 연결 또는 원본 검증 오류가 있어도 `변경 미리보기`를 실행할 수 있고, 빈 원본, 12개 관리 컬럼 헤더·필수값 누락, 잘못된 전화번호 또는 중복이 있으면 차단 상태로 표시되며 `변경 적용`은 비활성화된다.
- [ ] 변경이 있고 차단 오류가 없는 미리보기에서만 `변경 적용`이 활성화된다. 원본이 미리보기 이후 바뀌면 적용은 `409`로 거부되고 새 미리보기를 요구한다.
- [ ] 정상 적용은 추가·수정을 하나의 transaction으로 완료하고 기존 명부의 회원 연결과 DB에만 있는 행을 보존한다. 중간 오류나 동시 실행은 전체 무변경으로 종료된다.
- [ ] 비로그인 및 일반회원 `POST /api/payments` 요청이 각각 `401`, `403`을 반환하고 기록을 생성하지 않는다.
- [ ] 관리자가 합의된 테스트 결제 기록을 생성하면 `201`을 반환한다.
- [ ] Replit 실행 로그에 동문 원본 행, 이름, 전화번호, 주소, 이메일, 생일, 사용자 객체가 나타나지 않는다.

2026-07-27 현재 병합 후보의 내부 route-contract 자동화는 위 실제 관리자 결제 기록 생성을 실행하지 않고, valid `201`/write `1`, `amount` 누락 `400`/write `0`, 기존 `401/403` 무쓰기만 확인했다. payment provider·실제 결제와 Production smoke는 미검증이다.

### 후보 브라우저 QA의 방법 편차

- [x] IAB bootstrap 자산을 만들 수 없어 IAB 실행은 시작되지 않았다. 실제 격리 QA는 승인된 `agent-browser` CLI fallback으로 현재 병합 후보 Replit `dist/public` archive의 JS/CSS/index를 실행해 관리자 control count `1`, accessible name `관리자 화면으로 이동`, href `/admin`, 실제 click 후 `관리자 패널` heading `1`을 확인했다. member control은 `0`, direct `/admin`은 `접근 권한이 없습니다` heading `1`·`관리자 패널` `0`이었다. 후보 archive identity와 SHA-256은 후보별 `final-F3-browser/manifest.sha256` 및 manual QA evidence에 기록·검증되어 있다.
- [x] 같은 격리 시나리오에서 mutation/unexpected API/cross-origin/external asset/console·page·runtime error counts가 모두 `0`이었고, loopback·임시 추출물 cleanup 및 SHA-256 manifest 검증을 완료했다.
- [ ] native Development URL pre-navigation interception은 확인하지 못해 실행하지 않았고 `FAIL_BLOCKED`로 남긴다. 따라서 위 `[x]`는 `agent-browser` CLI synthetic/client-navigation QA일 뿐 IAB·native Development URL·실제 서버 응답·실제 계정 QA가 아니다.

근거: `최종 후보의 candidate-metadata.json으로 식별한 증거 루트 아래 final-F3-browser/`.

2026-07-27 운영 관리자 배지→`/admin` 스모크는 도구 privacy capability 미충족으로 기존 인증 페이지 attach 전 중단했다. 재로그인, 운영 mutation, Google Sheets 적용과 Production Database 변경은 실행하지 않았으므로 위 관리자 패널 진입 체크는 미완료로 유지하며, 실제 QA가 가능한 사용자가 추후 확인한다.

## Development 통합 QA 상태 (2026-07-16)

- [x] 실제 개발 카카오 회원 세션으로 로그인 후 회원 홈, 동문 주소록 범위·검색, `/events` 유형 전환·문자 분석·자동저장·새로고침 후 유형별 초안 복구·삭제를 확인했다. 이어 부고·결혼·개원·기타 네 유형을 실제 게시하고 목록과 `/events/:id` 상세를 확인했다. QA 게시물 ID 161~164와 후속 경고 검증 초안을 정리해 `community_events`는 0건으로 복구했다.
- [x] `/o`, `/o/new`가 각각 `/events?type=obituary`, `/events?type=obituary&compose=1`으로 이동하는 것을 확인했다. 390px 개발 브라우저에서 공개 홈페이지와 `/events`의 가로 overflow가 없고 브라우저 오류가 없는 것을 확인했다.
- [x] 부고 파서가 놓친 나이·관계를 직접 채웠을 때 상단 누락 경고가 즉시 사라지는지 실제 브라우저에서 확인했다. 회귀 테스트를 포함한 Replit 전체 311/311, `npm run check`, `npm run build`, `git diff --check`가 통과했다.
- [x] Development Database의 유일한 QA 회원을 트랜잭션으로 임시 관리자 승격한 뒤 관리자 패널·가입 승인 빈 상태·Google Sheets 연결·개인정보 없는 변경 미리보기를 확인했다. 원본·DB 3,458건, 차단 오류 3건에서 `변경 적용`이 비활성화됐고 실제 적용은 실행하지 않았다. 검증 뒤 회원 권한은 일반회원으로 복구했고 관리자 수는 0명이다.
- [x] 2026-07-18 Development와 Production Database에서 이름·정규화 전화번호가 정확히 일치하는 실제 카카오 회원을 각각 1명으로 확인하고, 카카오 회원번호 원문을 출력·커밋하지 않은 채 환경별 Replit Secret에 등록했다. 자동 복구·환경 선택·미설정·잘못된 형식·기존 관리자 비강등을 포함한 Replit 전체 테스트 317건, `npm run check`, `npm run build`, `git diff --check`가 통과했다.
- [x] 커뮤니티 홈의 관리자 배지를 `/admin` 링크로 바꾸고 관리자 조건·목적지·접근성 이름 계약을 추가했다. Replit 전체 테스트 318건, `npm run check`, `npm run build`, `git diff --check`가 통과했으며 실제 클릭은 다음 Republish 후 운영 관리자 세션에서 확인한다.
- [x] 프로필·활동지역·회원 상태의 세션 권한, 허용 필드, `ClientUser` 응답 축소, KST 당해연도 연회비 집계와 결제 요약 축소를 실제 Express/session route 계약 9개 시나리오와 KST 연도 경계·연회비 집계 테스트로 고정했다. Replit 집중 테스트 25/25, 전체 338/338, `npm run check`, `npm run build`, `git diff --check`가 모두 종료 코드 `0`으로 통과했다.
- [ ] 새 코드가 적용된 Development에서 재로그인 후 관리자 권한 자동 복구와 관리자 화면 진입을 확인한다. 이번 후보에서는 Development·Production 재로그인 복구를 재검증하지 않았으며, 아래 과거 기록은 후보 완료 근거로 사용하지 않는다.
- [ ] 이번 후보의 Development·Production allowlist 로그인, 관리자 권한 복구와 실제 Kakao 통합 QA를 확인한다. synthetic/client-navigation QA는 이 항목을 완료 처리하지 않는다.
- [ ] 실제 불일치 카카오 계정의 가입 대기·관리자 승인 또는 거절·카카오 연결 해제는 별도 불일치 테스트 계정이 필요하다.
- [ ] 관리자 결제 기록 생성은 테스트 금액·정리 범위를 확정한 뒤 실행한다.
- [ ] Production smoke, Google Sheets 적용, payment provider·실제 결제를 실제 계정과 운영 환경에서 확인한다.
- [ ] 운영 일반회원·관리자 역할, 모바일 네 유형 입력, 외부 링크 대표 사례를 묶어 최종 통합 QA한다.

## Production 배포 smoke 상태 (2026-07-16)

- [ ] 2026-07-27 Todo7 후보의 Production smoke는 실행하지 않았다. 아래의 이전 날짜 기록은 이번 후보의 운영·실계정 검증으로 재사용하지 않는다.

- [x] 2026-07-18 관리자 자동 복구 커밋 `e021457` Republish 후 운영 `/`, `/api/categories`가 `200`, 제거된 `POST /api/debug/login`이 `404`, 비로그인 `/api/admin/sync-alumni/preview`와 `/api/events`가 `401`을 반환했다. 기존 실제 회원 세션의 홈도 정상 로드됐다.
- [x] Republish 직후 `false`였던 지정 계정의 Production Database `isAdmin`이 운영 카카오 재로그인 뒤 `true`로 자동 복구됐고, 커뮤니티 홈의 `관리자` 표시를 실제 화면에서 확인했다. 관리자 패널 진입 링크는 다음 Republish 후 확인한다.
- [x] Republish 후 운영 `/`와 `/api/categories`가 `200`, 비로그인 `/api/admin/sync-alumni/preview`, `/api/events`, `POST /api/payments`가 `401`을 반환했다. 제거된 `POST /api/debug/login`과 미등록 `/api/*`는 고정 한국어 JSON `404`를 반환했다.
- [x] 운영 JavaScript 자산 `assets/index-CJN6DkYf.js`와 Replit 현재 빌드의 SHA-256이 `45d04488e7a34e2e7d4993a8aeff461196ca826bfcf63d8df136db92ea7a8f10`으로 일치해 배포본이 검증한 빌드와 같음을 확인했다.
- [x] 운영 실제 일반회원 세션으로 회원 홈, `/events`, `/directory`, `/o`의 `/events?type=obituary` 이동, `/profile`을 확인했다. 각 화면은 정상 제목과 경로로 로드됐고 1,280px 데스크톱에서 가로 overflow가 없었다.
- [x] 공개 홈 390px overflow 수정은 동일 배포 자산의 Development 390px 브라우저에서 `documentWidth=390`으로 확인했다. 운영의 별도 실제 모바일 화면 확인은 최종 통합 QA에 유지한다.
- [x] 이 smoke에서는 경조사 게시·결제 생성·명부 적용·Production Database 변경을 실행하지 않았다.
- [x] 2026-07-17 Republish 후 운영 자산 `assets/index--dq2g1qg.js`와 Replit 빌드의 SHA-256이 `42e384cf5e175deba7ba545979298798619b3279644bd0f5728959fc8fe289d1`로 일치했다. 실제 일반회원 세션의 홈·`/events`·`/directory`·`/o`·`/profile`을 390×844에서 확인했고 모든 화면의 `documentWidth`가 390px이며 브라우저 경고·오류가 없었다.
- [ ] 운영 일반회원 관리자 API `403`, 실제 관리자 성공, 모바일 네 유형 입력·게시·상세, 대표 공개 링크는 최종 통합 QA에서 확인한다.

## 카카오 동의·탈퇴 검증 상태 (2026-07-14)

- [x] Replit Development 격리 스냅샷에서 `npm test` 210/210, `npm run check`, `npm run build`, `git diff --check` 통과. 실제 PostgreSQL OAuth state·승인·거절·탈퇴 경쟁 테스트와 cleanup 0건을 포함한다.
- [x] 운영 `/login` 데스크톱·모바일 실제 브라우저 스크린샷과 `/privacy`, `/terms` 반영 상태 확인. 개인정보 없는 캡처는 `docs/review-assets/`에 보관한다.
- [x] 실제 개발 OAuth·명부 매칭·프로필 표시·탈퇴 smoke. 사용자·세션 0건, 명부 연결 해제, HMAC 종료 marker 2건과 비로그인 화면 복귀를 확인했다.
- [x] 카카오 심사용 PDF 생성 및 렌더링 검수. 제출 파일은 `docs/review-assets/kakao-consent-review.pdf`다.
- [x] 운영 additive 스키마, Republish와 비로그인 smoke. 공개 페이지·카테고리 `200`, 탈퇴·부고·경조사·업로드 보호 API `401`, 운영 명부 3,458건을 확인했다.
- [x] 카카오 운영 앱의 개인정보 동의항목 권한 심사에서 이름·전화번호 필수와 생일 선택 승인을 확인했다.
- [x] 승인된 이름·전화번호를 필수 동의, 생일을 선택 동의로 활성화하고 콘솔에 각 동의 목적을 저장했다.
- [x] 카카오톡 채널 추가 상태 및 내역을 선택 동의, 동의 목적 `동문회 공지 및 경조사 알림`으로 저장하고 동의 화면 미리보기에서 선택 항목을 확인했다.
- [x] 운영 OAuth 동의 화면에서 필수 이메일·전화번호·이름과 선택 프로필 사진·생일·채널 추가 상태를 확인하고, 실제 명부 매칭·선택정보 표시·회원 탈퇴를 완료했다. 탈퇴 후 운영 DB는 사용자·명부 연결·가입대기·세션 0건, 명부 3,458건 유지 상태다.
- [x] 국외이전 고지가 포함된 현재 HEAD를 Republish하고 운영 `/privacy`의 데스크톱·390px 모바일 표시와 콘솔 오류 없음을 확인했다.
- [x] 이름·전화번호 필수, 생일 선택, CI 사용 안 함과 심사용 PDF·운영 URL·수집 사유로 개인정보 동의항목 심사를 제출했고 2026-07-14 승인을 확인했다.
- [x] 운영 카카오 앱의 동의 화면에 동문회 카카오톡 채널이 대표 채널로 등록돼 있다. 채널 추가 상태 및 내역은 선택 동의로 사용하고, 친구 목록·카카오톡 메시지 접근권한은 사용하지 않는다.

## 배포 전후 기술 검증

Replit 개발 워크스페이스에서 실행한다.

```bash
npm test
npm run check
npm run build
git diff --check
```

네 명령의 기대 결과는 종료 코드 `0`이다. 기존 Browserslist와 번들 청크 크기 경고는 종료 코드가 `0`이면 허용한다.

이 검증만으로 경조사 통합의 UI, 파싱, 초안 복구 화면 또는 실제 데이터 마이그레이션 완료를 의미하지 않는다. 배포와 DB 변경은 검토 후 별도 게이트를 통과해야 한다.

운영 Republish 후에만 운영 URL에서 실행한다.

```bash
curl -I https://dgkma.org/
curl -sS https://dgkma.org/api/categories
curl -i https://dgkma.org/api/admin/sync-progress
curl -i -X POST https://dgkma.org/api/payments \
  -H "Content-Type: application/json" \
  -d '{"userId":1,"amount":1,"year":2026,"type":"verification","status":"pending"}'
curl -i -X POST https://dgkma.org/api/categories \
  -H "Content-Type: application/json" \
  -d '{"name":"deployment-probe","displayName":"probe"}'
```

기대 결과:

- 홈페이지는 `200`을 반환한다.
- 카테고리 API는 `all`, `notice`, `free`, `event`, `news`를 반환한다.
- 비로그인 관리자 진행 조회와 결제 기록 생성 요청은 `401`을 반환하며 데이터를 변경하지 않는다.
- 비로그인 카테고리 생성 요청은 `401`과 로그인 필요 메시지를 반환하며 데이터를 생성하지 않는다.

마지막으로 실제 계정에서 카카오 로그인, 게시글과 이미지, 댓글, 주소록, 부고의 변경된 흐름을 확인한다.
