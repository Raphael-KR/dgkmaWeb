# 개발 로드맵

이 문서는 현재 구현 상태를 기준으로 다음 개발 순서와 완료 조건을 관리합니다. 장기 제품 비전은 [planning_proposal.md](./planning_proposal.md), 프로덕션 검증 절차는 [walkthrough.md](./walkthrough.md)를 참고합니다.

## 상태 및 우선순위 정의

- `기능 검증 완료`: 핵심 사용자 흐름이 프로덕션에서 검증됨. 보안과 데이터 무결성 문제까지 모두 해결됐다는 의미는 아님
- `진행 중`: 현재 구현과 검증을 수행 중
- `진행 예정`: 다음 개발 대상으로 확정
- `정책 결정 필요`: 구현 전 운영 결정 필요
- `보류`: 선행 조건 이후 진행
- `P0`: 실서비스 보안 또는 핵심 데이터 무결성에 직접 영향을 주므로 다른 기능보다 먼저 처리
- `P1`: 개인정보 또는 접근 정책에 영향을 주며 P0 다음으로 처리

## 현재 완료된 기반

| 영역 | 상태 | 확인 근거 | 확인된 범위 | 주의·의존성 |
|---|---|---|---|---|
| 카카오 로그인·동의항목·회원 탈퇴 | 진행 중 | 코드·Replit 전체 자동화 검증 | REST OAuth 로그인, 원자 state 소비, 종료 경쟁 차단, 활동 지역 온보딩, 필수·선택 동의 범위, 생일 본인 전용 화면, 카카오 연결 해제 후 로컬 탈퇴 | 실제 개발 OAuth·탈퇴, 스크린샷·심사용 PDF, 운영 스키마 적용과 Republish가 남아 있음 |
| 동문 주소록 | 기능 검증 완료 | 프로덕션 검증 | 로그인, 기수·지역 범위 제한, 최소 필드 노출 | 없음 |
| 게시판 | 기능 검증 완료 | 프로덕션 검증 | 카테고리, 글 작성·상세, 검색, 댓글 작성·삭제, 이미지 첨부 | 없음 |
| 부고 기본 기능 | 기능 검증 완료 | 프로덕션 검증 | 목록, 작성, 상세, 정규식 기반 문자 파싱 | 경조사 통합 모델로 마이그레이션 예정 |
| 경조사 통합 기반/API | 진행 중 | 코드·Replit 검증 | `community_events` 저장 모델, 타입 계약, 회원 전용 published·draft API, 명시적 부고 마이그레이션 SQL 스크립트 | 개발·프로덕션 SQL 콘솔 적용과 데이터 검증은 별도 게이트 |
| 경조사 등록·초안 UI | 진행 중 | 코드·Replit 검증 | `/events` 항상 표시 등록 도구, 유형별 입력·오류, 텍스트 전용 파싱 안내, 자동저장·복구·삭제·재시도, 서버 데이터 기반 부고 미리보기·복사, 게시 뒤 목록 갱신, 게시판·회원 홈 진입점과 legacy 리디렉션 | Republish, 실제 회원 데스크톱·모바일 브라우저 검증, 프로덕션 데이터/마이그레이션 확인 전 |
| 회원 상태 표시 | 기능 검증 완료 | 코드·프로덕션 검증 | 내부 납부 기록 기반 당해년도 권리회원 표시 | P0 결제 기록 보호 전까지 납부 데이터 무결성을 신뢰할 수 없음 |

## 긴급

| 우선순위 | 과제 | 상태 | 확인 근거 | 완료 조건 | 선행 조건 |
|---|---|---|---|---|---|
| P0 | 관리자 API 보호 | 진행 중 | 코드·Replit 검증 | 모든 `/api/admin/*`가 비로그인 요청에 `401`, 일반회원 요청에 `403`, 관리자 요청에 성공 응답 | 공통 `requireAdmin` 정책 |
| P0 | 결제 기록 보호 | 진행 중 | 코드·Replit 검증 | 공개 결제 기록 생성이 차단되고 관리자 또는 검증된 결제 콜백만 기록을 생성 | 결제 기록 작성 주체 결정 |
| P1 | 개인정보 로그 제거 | 진행 중 | 코드·Replit 검증 | 동문 원본 행·이름·전화번호·주소·이메일·생일·사용자 객체가 로그에 출력되지 않고 건수·처리 단계·마스킹된 식별자만 기록 | 없음 |
| P1 | 부고 API 접근 정책 | 진행 중 | 프로덕션 비로그인 검증 | 목록·상세·파싱·등록의 비로그인 `401`과 실제 회원 흐름이 모두 검증됨 | 회원용 화면 진입점과 실제 계정 검증 |

## 다음

| 과제 | 상태 | 완료 조건 | 선행 조건 |
|---|---|---|---|
| 가입 불일치 정책 | 진행 중 | 관리자 승인 대기로 기획·API·화면 문구를 일치시킴 | 실제 불일치 계정 검증 |
| 카카오 동의항목 심사·회원 탈퇴 운영 검증 | 진행 중 | Replit 개발 워크스페이스의 현재 HEAD에서 `npm test`, `npm run check`, `npm run build`를 통과하고, 실제 개발 계정으로 OAuth·온보딩·탈퇴 후 새 연결의 `/api/auth/me` `401`, 사용자·세션 삭제, 명부 연결 해제, 콘텐츠 익명화를 확인 | 개발 워크스페이스 동기화와 SSH 접근, `KAKAO_DEV_ADMIN_KEY`, 테스트 계정 로그인 협조, 개인정보 없는 실제 화면 캡처; 운영 Republish 후 운영 환경은 별도 검증 |
| 게시판 분류 | 기능 검증 완료 | `공지`, `자유`, `행사`, `소식`과 예약 필터 `전체`를 기획서·화면·프로덕션 DB·seed 기준에 일치시킴 | 없음 |
| 경조사 통합 모델·마이그레이션 | 진행 중 | `부고`, `결혼`, `개원`, `기타` 통합 모델과 회원 전용 기반 API를 코드·Replit에서 검증하고, 기존 부고를 복사하는 명시적 SQL 스크립트를 준비 | 개발·프로덕션 SQL 콘솔 적용과 데이터 검증은 별도 게이트이며 [경조사 통합 설계](./docs/superpowers/specs/2026-07-11-community-events-design.md) 참고 |
| 경조사 한 화면 등록·목록 | 진행 중 | 코드·Replit 검증: 항상 노출되는 등록 도구, 유형 선택과 유형별 published 목록이 `/events` 한 화면에서 동작 | 실제 회원 데스크톱·모바일 브라우저 검증과 Republish |
| 경조사 초안 자동 저장 | 진행 중 | 코드·Replit 검증: 회원별 초안 저장·복구·삭제·재시도와 타 회원 접근 차단, 부고 미리보기가 동작 | 실제 회원 브라우저 검증과 운영 데이터 확인 |
| 경조사 문자·링크 파싱 | 진행 중 | 코드·Replit 검증: 입력한 문자만 분석하고 URL이 있으면 링크 수집 보류 안내를 표시 | 공개 링크 수집은 다음 파싱 계획으로 보류; 실패 fallback, SSRF·과대 응답·MIME 차단 설계·검증 필요 |
| 부고 표준 게시문 | 진행 예정 | 필수 정보 누락 차단, 본인상·회원 직함 규칙과 표준 템플릿이 작성 지침에 일치 | 경조사 초안 자동 저장 |
| PostgreSQL 기준 명부 일원화 | 진행 중 | 코드 기준 로그인 매칭은 PostgreSQL을 조회하고 Google Sheets는 명시적인 관리자 동기화에만 사용함. 실제 개발 OAuth와 운영 Republish 후 비의존성을 확인 | 실제 불일치 계정 검증 |
| 운영 DB 변경 절차 | 진행 예정 | 스키마 변경과 seed를 분리한 실행·검증·복구 절차가 문서화됨 | 명부 기준 확정 |

## 이후

| 과제 | 상태 | 완료 조건 | 선행 조건 |
|---|---|---|---|
| 알림톡·SMS | 보류 | 수신 동의, 발송 승인, 발송 이력, 실패 재시도가 검증됨 | 경조사 통합 모델·접근 정책 |
| 실제 결제 | 보류 | 결제 승인 콜백, 중복 방지, 실패·취소·환불, 영수증이 검증됨 | 결제 기록 보호 |
| 화환 주문 | 보류 | 주문 상태와 관리자 처리 흐름이 검증됨 | 실제 결제 또는 후불 정책 |
| 장학사업 | 보류 | 선발·지급·공시 범위와 개인정보 정책이 확정됨 | 운영 정책 확정 |
| 분석 | 보류 | 집계 지표와 개인정보 비식별 기준이 확정됨 | 데이터 품질 안정화 |

## 운영 원칙

- 기능 상태는 코드가 아니라 프로덕션 검증 결과를 기준으로 갱신합니다.
- 보안·개인정보·데이터 무결성 위험은 실제 운영 데이터 노출을 피하기 위해 코드 확인을 근거로 기록할 수 있습니다.
- `기능 검증 완료`는 보안 검토 완료를 의미하지 않으며, 관련 P0/P1 항목을 주의·의존성에 연결합니다.
- 정책 결정이 필요한 과제는 운영 결정을 문서화한 뒤 구현합니다.
- 외부 연동은 성공 응답뿐 아니라 실패, 재시도, 중복, 취소 흐름까지 완료 조건에 포함합니다.
- 데이터베이스 변경은 개발 DB에서 검증한 후 프로덕션 SQL 콘솔에서 별도로 적용하고 결과를 확인합니다.
- 경조사 기반의 코드·API·마이그레이션 스크립트 진행 상태는 개발·프로덕션 데이터 마이그레이션 완료와 구분합니다. 스키마와 데이터 SQL은 승인된 SQL 콘솔 절차를 통과하기 전까지 적용 완료로 기록하지 않습니다.
- 경조사 등록·초안 UI의 `코드·Replit 검증`은 실제 회원 브라우저 검증, Republish, 프로덕션 데이터 확인을 포함하지 않습니다. 이 항목들은 관찰된 운영 결과가 있을 때만 `기능 검증 완료`로 갱신합니다.
- 카카오 동의항목·회원 탈퇴의 과거 집중 테스트는 현재 HEAD가 개발 Replit에 동기화됐음을 뜻하지 않습니다. 현재 HEAD의 전체 자동화, 실제 OAuth·탈퇴, 스크린샷과 심사용 PDF가 확인되기 전에는 `기능 검증 완료` 또는 심사 제출 완료로 갱신하지 않습니다.
- 담당자와 목표일은 실제 배정과 일정이 확정된 과제부터 GitHub Issue 또는 프로젝트 보드에서 관리합니다.
